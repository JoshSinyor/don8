import React from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

const Ad = (props) => {
  return(
    <View>
      <Text> {props.name} </Text>
    </View>
  )
}


export default Ad;
